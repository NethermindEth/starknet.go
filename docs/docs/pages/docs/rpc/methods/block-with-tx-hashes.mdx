# BlockWithTxHashes

Gets a block with only transaction hashes (not full transaction details).

## Method Signature

```go
func (provider *Provider) BlockWithTxHashes(ctx context.Context, blockID BlockID) (interface{}, error)
```

## Parameters

- `ctx` - Context for request cancellation and timeout
- `blockID` - Block identifier (number, hash, or tag)

## Returns

- `interface{}` - Block with transaction hashes only
- `error` - Error if the request fails

## Usage Example

```go
package main

import (
    "context"
    "encoding/json"
    "fmt"
    "log"

    "github.com/NethermindEth/starknet.go/rpc"
)

func main() {
    // Create RPC client
    client, err := rpc.NewProvider("https://starknet-sepolia.public.blastapi.io/rpc/v0_8")
    if err != nil {
        log.Fatal("Failed to create client:", err)
    }

    // Get latest block with transaction hashes
    blockID := rpc.WithBlockTag("latest")
    block, err := client.BlockWithTxHashes(context.Background(), blockID)
    if err != nil {
        log.Fatal("Failed to get block:", err)
    }

    // Pretty print the result
    blockJSON, _ := json.MarshalIndent(block, "", "  ")
    fmt.Printf("Block with transaction hashes:\n%s\n", blockJSON)
}
```

## Expected Output

```json
{
  "block_hash": "0x501d946a49a17460aac51cb8f7ed4a1aaa9cb58fd750dccb30159e55985b7aa",
  "parent_hash": "0x67462dab6b244394e61b0786841aa93417dcd67baeba0e4bbbe11635d653ae1",
  "block_number": 1675778,
  "new_root": "0x3f6ad70de865fcb8678c780cc49101ea6ff9209936212e12459b5255a1ee494",
  "timestamp": 1755961667,
  "sequencer_address": "0x1176a1bd84444c89232ec27754698e5d2e7e1a7f1539f12027f28b23ec9f3d8",
  "l1_gas_price": {
    "price_in_fri": "0x222754d09832",
    "price_in_wei": "0x3c23deae"
  },
  "l2_gas_price": {
    "price_in_fri": "0xb2d05e00",
    "price_in_wei": "0x13ade"
  },
  "l1_data_gas_price": {
    "price_in_fri": "0x9161",
    "price_in_wei": "0x1"
  },
  "l1_da_mode": "BLOB",
  "starknet_version": "0.14.0",
  "status": "ACCEPTED_ON_L2",
  "transactions": [
    "0x4a32032be9e8cc03cb1a51169366f18ade7292cddb3c1056f8634ff8d2fe6f9"
  ]
}
```

This block contains 1 transaction with only the hash shown (instead of full transaction details).

## Get Historical Block Example

```go
// Get a specific historical block
blockNumber := uint64(1000000)
blockID := rpc.WithBlockNumber(blockNumber)

block, err := client.BlockWithTxHashes(context.Background(), blockID)
if err != nil {
    log.Fatal("Failed to get historical block:", err)
}

fmt.Printf("Retrieved historical block %d\n", blockNumber)
```

## Use Cases

- **Quick Block Overview** - Get block information without transaction details
- **Efficient Monitoring** - Monitor blocks with minimal data transfer
- **Transaction Reference** - Get transaction hashes for separate detailed queries
- **Lightweight Block Explorers** - Build fast block browsing interfaces
- **Bulk Processing** - Process many blocks efficiently
- **Network Analytics** - Analyze block patterns and transaction volumes

## Performance Benefits

- **Faster Response** - Much quicker than BlockWithTxs()
- **Reduced Bandwidth** - Significantly less data transfer
- **Lower Memory Usage** - Minimal memory requirements
- **Efficient for Monitoring** - Perfect for real-time block tracking

## When to Use vs BlockWithTxs

**Use BlockWithTxHashes when:**
- You need basic block information
- You want to minimize data transfer
- You're processing many blocks
- You only need transaction hashes for reference

**Use BlockWithTxs when:**
- You need complete transaction details
- You're analyzing transaction contents
- You want all data in a single call

## Error Handling

```go
block, err := client.BlockWithTxHashes(ctx, blockID)
if err != nil {
    log.Printf("Error getting block with tx hashes: %v", err)
    return
}

fmt.Printf("Successfully retrieved block with transaction hashes\n")
```