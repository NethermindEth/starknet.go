# Get Block With Transactions

Returns the block information along with full transaction details.

## Parameters

- `block_id` - The block identifier. Can be:
  - Block hash
  - Block number
  - "latest" for the latest block
  - "pending" for the pending block

## Returns

Returns a block object containing:
- Block header information
- Full transaction details
- Other block-related data

## Example

```go
package main

import (
	"context"
	"fmt"
	"log"
	"github.com/NethermindEth/starknet.go/rpc"
)

func main() {
	rpcUrl := "https://starknet-mainnet.public.blastapi.io/rpc/v0_8"
	client, err := rpc.NewClient(rpcUrl)
	if err != nil {
		log.Fatal(err)
	}

	provider := rpc.NewProvider(client)
	result, err := provider.BlockWithTxs(context.Background(), rpc.BlockID{Tag: "latest"})
	if err != nil {
		log.Fatal(err)
	}
	fmt.Println("BlockWithTxs:", result)
}
```

## Error Cases

- `BLOCK_NOT_FOUND` - If the specified block does not exist
- `INVALID_BLOCK_ID` - If the block identifier is invalid 
